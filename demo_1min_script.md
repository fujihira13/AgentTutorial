# 1分デモ動画台本（4コマ漫画エージェント）

## 事前準備（録画前）
- `.env` に `GOOGLE_API_KEY` を設定
- `adk web comic_agent` が起動できる状態にする
- 待ち時間短縮のため、`output/` を一度生成しておく
- 入力文を固定してメモしておく（例: 「テーマ：コーヒーショップ、登場人物：ロボットと店員、雰囲気：コメディ」）

## タイムライン（60秒）
00-05秒: タイトル表示「4コマ漫画エージェント（ADK + Gemini）」  
05-15秒: ひとこと説明「入力からストーリー・画像・音声・HTMLまで一気通貫で生成」  
15-25秒: `adk web comic_agent` 起動画面 → 入力欄にテーマを入力  
25-40秒: `output/` フォルダを開き `comic.json` と `comic.png` をチラ見せ  
40-55秒: `index.html` を開き、ビューアと音声再生を見せる  
55-60秒: まとめ「ADKでツール連携を実装、フォールバックも備えて安定化」  

## ナレーション例（読み上げ）
「Google ADKで4コマ漫画エージェントを作りました。  
入力を与えると、ストーリー生成、4コマ画像、音声、HTMLビューアまで自動で出力します。  
実行後は `output/` に `comic.json` と `comic.png` が生成され、  
`index.html` を開くと漫画と音声をすぐ確認できます。  
4コマJSONの検証やフォールバックで安定動作させているのがポイントです。」  

## 画面構成のポイント
- 生成待ちが長い場合は、事前生成済みの `output/` を見せる
- `comic.json` の「起承転結」が分かる部分を一瞬だけ映す
- 最後は `index.html` の再生画面で締める
